sdr:
  # Gain index for RTL-SDR R820T tuner. NOT a dB value — it's a zero-based index.
  # -1 = AGC (auto). Valid range: 0-28 for R820T.
  #
  # Index → dB mapping:
  #   0=0.0  1=0.9  2=1.4  3=2.7  4=3.7  5=7.7  6=8.7  7=12.5
  #   8=14.4  9=15.7  10=16.6  11=19.7  12=20.7  13=22.9  14=25.4
  #   15=28.0  16=29.7  17=32.8  18=33.8  19=36.4  20=37.2  21=38.6
  #   22=40.2  23=42.1  24=43.4  25=43.9  26=44.5  27=48.0  28=49.6
  #
  # Recommended for DAB+ urban reception: 20 (37.2 dB)
  # If no stations found, auto-gain detection will probe different values.
  # The best value will be saved back to this file automatically.
  gain: 20
  driver: "auto"  # auto, rtl_sdr, rtl_tcp, soapysdr

server:
  port: 8800
  host: "0.0.0.0"

welle_cli:
  internal_port: 7979  # welle-cli's own web server port (not exposed to user)
  binary_path: null  # null = auto-detect via PATH. Override with absolute path if needed.

scanning:
  # Canberra active blocks first, then other Australian markets
  priority_blocks: ["8D", "9C", "8C", "9A", "9B", "9D"]
  dwell_time: 10  # seconds per block (minimum 8)
  poll_interval: 2  # seconds between mux.json polls during dwell
  full_scan: false  # set true to scan all 38 Band III blocks
  retry_empty: true  # retry blocks that returned zero services

mock_mode: false  # set true for development without SDR hardware
